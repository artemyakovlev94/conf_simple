
#Область ПрограммныйИнтерфейс

Функция УстановитьХранениеЛогов(КоличествоДней) Экспорт
	
	ПараметрКоличествоДней = ОбщегоНазначения.ОписаниеТипаЧисло(2, , ДопустимыйЗнак.Неотрицательный).ПривестиЗначение(КоличествоДней);
	Константы.SUI_ХранениеЛоговHTTPЗапросовВДнях.Установить(ПараметрКоличествоДней);
	Возврат ПараметрКоличествоДней;
	
КонецФункции

Процедура ИспользованиеЛогирования(Знач Используется) Экспорт
	
	Используется = SUI_ОбщегоНазначенияКлиентСервер.ОписаниеТипаБулево().ПривестиЗначение(Используется);
	
	Константы.SUI_ЛогированиеHTTPЗапросов.Установить(Используется);
	
КонецПроцедуры

Функция ВыгрузитьЛогированиеHTTPЗапросовВФайл(ПараметрыВыгрузки) Экспорт
	
	Возврат ДлительныеОперации.ВыполнитьВФоне("SUI_ЛогированиеHTTPЗапросов.ВыгрузитьЛогированиеHTTPЗапросовВФайл", 
		ПараметрыВыгрузки, ДлительныеОперации.ПараметрыВыполненияВФоне());
	
КонецФункции

Функция ЛогированиеHTTPСервисаДоступно(ОтносительныйАдрес) Экспорт
	
	ИмяШаблона = SUI_HTTPСервисПовтИсп.ОписаниеДанныхHTTPСервисаExchange()
		.Получить("Адреса").Получить(ОтносительныйАдрес);
	
	Если ИмяШаблона <> Неопределено И ПолучитьФункциональнуюОпцию("SUI_ЛогированиеHTTPЗапросов") Тогда
		ПараметрыФО = Новый Структура("SUI_ПараметрыЛогированияHTTPСервисаИмяШаблона", ИмяШаблона);
		Возврат Не ПолучитьФункциональнуюОпцию("SUI_ОтключитьЛогированиеШаблонаHTTPСервиса", ПараметрыФО);
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Процедура ЗаписатьСообщениеЛогирования(СообщениеЛогирования) Экспорт
	
	Запись = РегистрыСведений.SUI_ЛогированиеHTTPЗапросов.СоздатьМенеджерЗаписи();
		
	Запись.Период = ТекущаяДатаСеанса();
	Запись.SID = Строка(Новый УникальныйИдентификатор());
	ЗаполнитьЗначенияСвойств(Запись, СообщениеЛогирования);
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запись.МеткаВремени = ТекущаяУниверсальнаяДатаВМиллисекундах();
	Запись.НомерСеанса = НомерСеансаИнформационнойБазы();
	Запись.ИмяПользователя = ИмяПользователя();
	
	ТекущийПользователь = Пользователи.ТекущийПользователь();
	Если ЗначениеЗаполнено(ТекущийПользователь) Тогда
		Запись.ИдентификаторПользователя = Строка(ОбщегоНазначения.ЗначениеРеквизитаОбъекта(
			ТекущийПользователь, "ИдентификаторПользователяИБ"));
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Ложь);
	
	Запись.Записать(Истина);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

Функция HTTPМетодПоСтроке(HTTPМетодСтрокой) Экспорт
	Возврат SUI_ОбщегоНазначенияПереопределяемый.ПредопределенныйЭлемент("Перечисление.SUI_HTTPМетоды." + HTTPМетодСтрокой);
КонецФункции

Функция ПреобразованиеДанныхJSON(Свойство, Значение, ДополнительныеПараметры, Отказ) Экспорт
	Возврат SUI_ОбщегоНазначения.ПреобразованиеДанныхJSON(Свойство, Значение, ДополнительныеПараметры, Отказ);
КонецФункции

#КонецОбласти
